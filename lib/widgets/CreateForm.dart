import 'package:flutter/material.dart';

class CreateAlarmFrom extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Alarm'),
        backgroundColor: Color(0xFF00E676),
      ),
      body: Text('Createform'),
    );
  }
}